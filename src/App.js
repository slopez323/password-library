import "./App.css";
import { CreatePassword } from "./components/create-password";

// xx clean up files and create relevant components
// xx page structure - main component (with description); password inputs
// xx check both inputs matching
// xx validate on submit
// show invalid reqs
// some styling
// accessibility
// tests

function App() {
  return <CreatePassword />;
}

export default App;
